/**
 * @deprecated Importe de `../features/comments` (ou `../../features/comments`)
 * Esta reexportação existe apenas para compatibilidade com imports antigos.
 */
export * from '../features/comments/commentService';
